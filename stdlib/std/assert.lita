import "std/string"
import "std/libc"
// import "std/thread"

#if TESTING
    import "std/test"
    public func assert(
        cond: bool,
        filename: *const char = __FILE__,
        lineNumber: usize = __LINE__
    ) {
        Assert(cond, filename, lineNumber)
    }
#else
    @include("assert.h");
    @foreign public func assert(e:bool):void;
#end


