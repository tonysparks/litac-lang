import "std/libc"
import "std/assert"

@raw("// hi");

@raw("""
#define MAX_PATH (260)
""");
@foreign
const MAX_PATH: i32;

struct Test {
                    @alias("brett")
                    v: i32
                }

func main(len: i32, args: **char) : i32 {

    //var ¥= 10
    //printf("Hello %d\n", ¥)
    printf("Hello world\n")
    var filename = [MAX_PATH]char{0}

    var t = Test{
                    .v = 4
                }

                assert(t.v == 4)
}
