{
    "description": "RandomAPI",
    "disabled": false,
    "program": `
        @include("assert.h");
        @foreign func assert(e:bool):void;

        %definitions%

        func main(len:i32, args:**char):i32 {
            %code%
    }
    `,
    "tests": [
        {
            "name": "Test Random",
            "definitions": `
                import "std/crypto/random"
            `,
            "code": `
                var x = SecureRandom64()
                var y = SecureRandom64()
                assert(x != y)
            `,
        },
    ]
}